
package package1;

/**
 *
 * @author Eza
 */
public class Tester4 {
    public static void main(String[] args) {
        PublicModifier publicmodifier = new PublicModifier();
        publicmodifier.kali();
        publicmodifier.tambah();
        publicmodifier.kurang();
        publicmodifier.bagi();
        publicmodifier.rata();
    }
}
/* method " kali() " dapat diakses karena type data public bisa diakses semua class yang menggunakan class yang berisi variabel tsb. 
*/
